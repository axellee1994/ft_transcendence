{
    "name": "test-project",
    "dependencies": {
      "fastify": "^4.0.0",           // This is allowed
      "typescript": "^4.0.0",        // This is allowed
      "tailwindcss": "^3.0.0",      // This is allowed
      "react": "^18.0.0",           // Should trigger violation
      "express": "^4.0.0",          // Should trigger violation
      "vue": "^3.0.0"               // Should trigger violation
    }
  }